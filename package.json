{
  "name": "Plugins",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.4"
  },
  "scripts": {
    "inject": "yarn build && node index.js",
    "build": "yarn build:eventviewer",
    "build:eventviewer": "tsc --build plugins/eventviewer"
  }
}